<%= form_with(model: serie, class: "custom-form") do |form| %>
  <% if serie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(serie.errors.count, "error") %> prohibited this serie from being saved:</h2>

      <ul>
        <% serie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class: "custom-form">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class: "custom-form">
    <%= form.label :synopsis, style: "display: block" %>
    <%= form.text_area :synopsis %>
  </div>

  <div class: "custom-form">
    <%= form.label :director, style: "display: block" %>
    <%= form.text_field :director %>
  </div>

  <div class="submit-button">
    <%= form.submit %>
  </div>
<% end %>
